apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: poc-omnigent
  labels:
    app: postgres
    app.kubernetes.io/managed-by: autopoc
    autopoc.io/project: omnigent
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
